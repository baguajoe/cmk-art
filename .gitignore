# Dependencies
node_modules/
client/node_modules/

# Python virtual environment
venv/
server/venv/
__pycache__/
server/__pycache__/
*.pyc

# Local data file from the optional legacy Flask server (server/app.py). The
# live site is static and does not use it.
contacts.json
server/contacts.json

# Build output
client/dist/
dist/

# Environment / editor. The static site has no secrets or .env files, but keep
# .env ignored so nothing sensitive is ever committed by accident.
.env
.env.*
.DS_Store
.vscode/
